---
import { getCollection } from "astro:content";
// Fuente canónica: Ipialabogados.md §3.1/§3.2/§3.3/§4 — getCollection drives rendering; no hardcode arrays (§6).
const servicios = (await getCollection("servicios")).sort((a, b) => a.data.order - b.data.order);

function parseItems(body: string): string[] {
  return body
    .split("\n")
    .map((l) => l.trim())
    .filter((l) => l.startsWith("- "))
    .map((l) => l.slice(2).trim())
    .filter(Boolean);
}

const byGrupo = new Map(servicios.map((e) => [e.data.grupo, { entry: e, items: parseItems(e.body ?? "") }]));
const trabajadores = byGrupo.get("trabajadores");
const empleadores = byGrupo.get("empleadores");
const pensionados = byGrupo.get("pensionados");
const penal = byGrupo.get("penal");

const omarGrupos = [trabajadores, empleadores, pensionados].filter(Boolean) as NonNullable<typeof trabajadores>[];
---

<section id="servicios" class="py-12 md:py-20 bg-surface section-divider">
  <div class="max-w-[1200px] mx-auto px-6 md:px-10">
    <div class="text-center mb-16">
      <h2 class="text-[32px] leading-10 font-semibold text-on-surface underline-motif underline-motif--center">Servicios Jurídicos Especializados</h2>
    </div>

    <div class="services-board">
      <!-- Omar Enrique Ipial Ipial — Laboral y Seguridad Social §3 -->
      <div data-abogado="omar" class="mb-16">
        <div class="flex flex-col md:flex-row md:items-baseline gap-2 mb-8 border-b border-platinum-silver pb-4">
          <h3 class="text-[24px] font-semibold text-on-surface">Omar Enrique Ipial Ipial</h3>
          <p class="text-xs font-semibold text-primary tracking-widest uppercase">Especialista en Derecho Laboral y Seguridad Social</p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-12">
          {omarGrupos.map((g) => {
            const grupo = g.entry.data.grupo;
            const title = g.entry.data.title;
            const items = g.items;
            const first = items.slice(0, 6);
            const rest = items.slice(6);
            const hasMore = rest.length > 0;
            return (
              <div data-grupo={grupo} data-abogado="omar" class="flex flex-col">
                <h4 class="text-[18px] font-semibold text-on-surface mb-4 border-l-2 border-l-primary pl-3">{title}</h4>
                <ul class="space-y-0">
                  {first.map((item) => (
                    <li class="text-[14px] leading-6 text-on-surface border-b border-platinum-silver/60 py-2">{item}</li>
                  ))}
                </ul>
                {hasMore && (
                  <>
                    <div id={`grupo-${grupo}`} class="grupo-extra">
                      <ul class="space-y-0 mt-0">
                        {rest.map((item) => (
                          <li class="text-[14px] leading-6 text-on-surface border-b border-platinum-silver/60 py-2">{item}</li>
                        ))}
                      </ul>
                    </div>
                    <button data-grupo-toggle aria-expanded="false" aria-controls={`grupo-${grupo}`} class="mt-4 text-xs font-semibold text-primary hover:text-primary-container transition-colors duration-200 uppercase tracking-wider self-start focus-visible:outline-2 focus-visible:outline-primary focus-visible:outline-offset-2">
                      Leer más
                    </button>
                  </>
                )}
              </div>
            );
          })}
        </div>
      </div>

      <!-- Franco Miller Ipial Ipial — Penal §4 -->
      {penal && (
        <div data-abogado="franco" class="mt-16">
          <div class="flex flex-col md:flex-row md:items-baseline gap-2 mb-8 border-b border-platinum-silver pb-4">
            <h3 class="text-[24px] font-semibold text-on-surface">Franco Miller Ipial Ipial</h3>
            <p class="text-xs font-semibold text-primary tracking-widest uppercase">Especialista en Derecho Penal y Procesal Penal</p>
          </div>
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12">
            <div data-grupo="penal" data-abogado="franco" class="flex flex-col">
              <h4 class="text-[18px] font-semibold text-on-surface mb-4 border-l-2 border-l-primary pl-3">{penal.entry.data.title}</h4>
              <ul class="space-y-0">
                {penal.items.map((item) => (
                  <li class="text-[14px] leading-6 text-on-surface border-b border-platinum-silver/60 py-2">{item}</li>
                ))}
              </ul>
            </div>
          </div>
        </div>
      )}
    </div>
  </div>
</section>

<script>
  (() => {
    const toggles = document.querySelectorAll("[data-grupo-toggle]");
    toggles.forEach((btn) => {
      btn.addEventListener("click", () => {
        const id = btn.getAttribute("aria-controls");
        const panel = id ? document.getElementById(id) : null;
        const expanded = btn.getAttribute("aria-expanded") === "true";
        btn.setAttribute("aria-expanded", String(!expanded));
        if (panel) panel.classList.toggle("is-expanded", !expanded);
        btn.textContent = expanded ? "Leer más" : "Leer menos";
      });
    });
  })();
</script>
