---
// Fuente: Ipialabogados.md §3.1/§3.2/§3.3/§4 — fuente canónica, transcripción verbatim §6. No invención.
const omarTrabajadores = [
  "Derechos de petición.", // §3.1
  "Acciones de tutela.", // §3.1
  "Conciliaciones laborales.", // §3.1
  "Representación y asesoría en procesos disciplinarios.", // §3.1
  "Trámites pensionales.", // §3.1
  "Revisión y liquidación de prestaciones sociales.", // §3.1
  "Cobro de incapacidades ante entidades de seguridad social.", // §3.1
  "Demandas y tutelas por fueros de estabilidad laboral reforzada.", // §3.1
  "Demandas por accidentes o enfermedades laborales.", // §3.1
  "Demandas laborales por violación al fuero sindical y circunstancial.", // §3.1
  "Demandas laborales por contrato realidad.", // §3.1
  "Demandas por no pago de prestaciones sociales.", // §3.1
  "Demandas por despidos sin justa causa.", // §3.1
] as const; // §3.1 Servicios para trabajadores

const omarEmpleadores = [
  "Asesoría en pensiones.", // §3.2
  "Asesoría en seguridad social.", // §3.2
  "Asesoría en derecho laboral.", // §3.2
  "Asesoría en vinculaciones laborales.", // §3.2
  "Asesoría en desvinculaciones laborales.", // §3.2
  "Elaboración y asesoría sobre contratos laborales.", // §3.2
  "Reglamentos internos de trabajo.", // §3.2
  "Asesoría en procesos disciplinarios.", // §3.2
  "Asesoría en manejo de incapacidades.", // §3.2
  "Asesoría en trabajadores con fueros de estabilidad laboral reforzada.", // §3.2
  "Representación y defensa en procesos judiciales.", // §3.2
  "Capacitaciones y actualización en la normatividad laboral.", // §3.2
] as const; // §3.2 Servicios para empleadores

const omarPensionados = [
  "Reliquidaciones pensionales.", // §3.3
  "Estudios pensionales.", // §3.3
  "Cálculos actuariales.", // §3.3
  "Asesoría y trámite para reconocimiento de pensión de vejez.", // §3.3
  "Asesoría y trámite para reconocimiento de pensión de sobrevivientes.", // §3.3
  "Asesoría y trámite para reconocimiento de pensión de invalidez.", // §3.3
  "Sustituciones pensionales.", // §3.3
  "Devolución de aportes.", // §3.3
  "Demandas por ineficacia del traslado.", // §3.3
  "Demandas para el reconocimiento de pensiones.", // §3.3
] as const; // §3.3 Servicios para pensionados

const francoPenal = [
  "Representación judicial y asesoría en asuntos y procesos penales.", // §4
  "Defensa y representación en audiencias preliminares.", // §4
  "Defensa y representación en audiencias de juicio oral.", // §4
  "Sustitución de medidas de aseguramiento.", // §4
  "Trámite para obtener libertad condicional.", // §4
  "Trámite para obtener prisión domiciliaria.", // §4
  "Devolución provisional de vehículos.", // §4
  "Devolución definitiva de vehículos.", // §4
  "Traslados a resguardos indígenas.", // §4
  "Representación de víctimas.", // §4
] as const; // §4 Servicios de Derecho Penal y Procesal Penal
---
<section id="servicios" class="py-20 bg-surface section-divider">
  <div class="max-w-[1200px] mx-auto px-6 md:px-16">
    <div class="text-center mb-16">
      <h2 class="text-[32px] leading-10 font-semibold text-on-surface underline-motif underline-motif--center">Servicios Jurídicos Especializados</h2>
    </div>

    <div class="services-board">
      <!-- Omar Enrique Ipial Ipial — Laboral y Seguridad Social §3 -->
      <div data-abogado="omar" class="mb-16">
        <div class="flex flex-col md:flex-row md:items-baseline gap-2 mb-8 border-b border-outline-variant pb-4">
          <h3 class="text-[24px] font-semibold text-on-surface">Omar Enrique Ipial Ipial</h3>
          <p class="text-xs font-semibold text-primary tracking-widest uppercase">Especialista en Derecho Laboral y Seguridad Social</p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-12">
          <div data-grupo="trabajadores" class="flex flex-col">
            <h4 class="text-[18px] font-semibold text-on-surface mb-4 border-l-2 border-l-primary pl-3">Para trabajadores</h4>
            <ul class="space-y-3">
              {omarTrabajadores.slice(0, 6).map((item) => (
                <li class="text-[14px] leading-6 text-on-surface border-b border-outline-variant/50 pb-2">{item}</li>
              ))}
            </ul>
            <div id="grupo-trabajadores" class="grupo-extra">
              <ul class="space-y-3 mt-3">
                {omarTrabajadores.slice(6).map((item) => (
                  <li class="text-[14px] leading-6 text-on-surface border-b border-outline-variant/50 pb-2">{item}</li>
                ))}
              </ul>
            </div>
            <button data-grupo-toggle aria-expanded="false" aria-controls="grupo-trabajadores" class="mt-4 text-xs font-semibold text-primary hover:text-primary-container transition-colors duration-200 uppercase tracking-wider">Leer más</button>
          </div>

          <div data-grupo="empleadores" class="flex flex-col">
            <h4 class="text-[18px] font-semibold text-on-surface mb-4 border-l-2 border-l-primary pl-3">Para empleadores</h4>
            <ul class="space-y-3">
              {omarEmpleadores.slice(0, 6).map((item) => (
                <li class="text-[14px] leading-6 text-on-surface border-b border-outline-variant/50 pb-2">{item}</li>
              ))}
            </ul>
            <div id="grupo-empleadores" class="grupo-extra">
              <ul class="space-y-3 mt-3">
                {omarEmpleadores.slice(6).map((item) => (
                  <li class="text-[14px] leading-6 text-on-surface border-b border-outline-variant/50 pb-2">{item}</li>
                ))}
              </ul>
            </div>
            <button data-grupo-toggle aria-expanded="false" aria-controls="grupo-empleadores" class="mt-4 text-xs font-semibold text-primary hover:text-primary-container transition-colors duration-200 uppercase tracking-wider">Leer más</button>
          </div>

          <div data-grupo="pensionados" class="flex flex-col">
            <h4 class="text-[18px] font-semibold text-on-surface mb-4 border-l-2 border-l-primary pl-3">Para pensionados</h4>
            <ul class="space-y-3">
              {omarPensionados.slice(0, 6).map((item) => (
                <li class="text-[14px] leading-6 text-on-surface border-b border-outline-variant/50 pb-2">{item}</li>
              ))}
            </ul>
            <div id="grupo-pensionados" class="grupo-extra">
              <ul class="space-y-3 mt-3">
                {omarPensionados.slice(6).map((item) => (
                  <li class="text-[14px] leading-6 text-on-surface border-b border-outline-variant/50 pb-2">{item}</li>
                ))}
              </ul>
            </div>
            <button data-grupo-toggle aria-expanded="false" aria-controls="grupo-pensionados" class="mt-4 text-xs font-semibold text-primary hover:text-primary-container transition-colors duration-200 uppercase tracking-wider">Leer más</button>
          </div>
        </div>
      </div>

      <!-- Franco Miller Ipial Ipial — Penal §4 -->
      <div data-abogado="franco" class="mt-16">
        <div class="flex flex-col md:flex-row md:items-baseline gap-2 mb-8 border-b border-outline-variant pb-4">
          <h3 class="text-[24px] font-semibold text-on-surface">Franco Miller Ipial Ipial</h3>
          <p class="text-xs font-semibold text-primary tracking-widest uppercase">Especialista en Derecho Penal y Procesal Penal</p>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12">
          <div data-grupo="penal" class="flex flex-col">
            <h4 class="text-[18px] font-semibold text-on-surface mb-4 border-l-2 border-l-primary pl-3">Servicios de Derecho Penal</h4>
            <ul class="space-y-3">
              {francoPenal.map((item) => (
                <li class="text-[14px] leading-6 text-on-surface border-b border-outline-variant/50 pb-2">{item}</li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  (() => {
    const toggles = document.querySelectorAll('[data-grupo-toggle]');
    toggles.forEach((btn) => {
      btn.addEventListener('click', () => {
        const id = btn.getAttribute('aria-controls');
        const panel = id ? document.getElementById(id) : null;
        const expanded = btn.getAttribute('aria-expanded') === 'true';
        btn.setAttribute('aria-expanded', String(!expanded));
        if (panel) panel.classList.toggle('is-expanded', !expanded);
        btn.textContent = expanded ? 'Leer más' : 'Leer menos';
      });
    });
  })();
</script>3 -->
      <div data-abogado="omar" class="mb-12">
        <h3 class="text-[22px] font-semibold text-on-surface mb-1">Omar Enrique Ipial Ipial</h3>
        <p class="text-xs font-semibold text-primary tracking-widest uppercase mb-6">Especialista en Derecho Laboral y Seguridad Social</p>

        <div data-grupo="trabajadores" class="mb-8">
          <h4 class="text-[18px] font-semibold text-on-surface mb-3">Para trabajadores</h4>
          <ul class="grid grid-cols-1 sm:grid-cols-2 gap-3">
            {omarTrabajadores.slice(0, 6).map((item) => (
              <li class="bg-white border border-outline-variant rounded-sm p-4 border-l-2 border-l-primary text-[14px] leading-6 text-on-surface">{item}</li>
            ))}
          </ul>
          <div id="grupo-trabajadores" class="grupo-extra">
            <ul class="grid grid-cols-1 sm:grid-cols-2 gap-3 mt-3">
              {omarTrabajadores.slice(6).map((item) => (
                <li class="bg-white border border-outline-variant rounded-sm p-4 border-l-2 border-l-primary text-[14px] leading-6 text-on-surface">{item}</li>
              ))}
            </ul>
          </div>
          <button data-grupo-toggle aria-expanded="false" aria-controls="grupo-trabajadores" class="mt-3 text-sm font-semibold text-primary hover:text-primary-container transition-[opacity,transform] duration-200">Leer más</button>
        </div>

        <div data-grupo="empleadores" class="mb-8">
          <h4 class="text-[18px] font-semibold text-on-surface mb-3">Para empleadores</h4>
          <ul class="grid grid-cols-1 sm:grid-cols-2 gap-3">
            {omarEmpleadores.slice(0, 6).map((item) => (
              <li class="bg-white border border-outline-variant rounded-sm p-4 border-l-2 border-l-primary text-[14px] leading-6 text-on-surface">{item}</li>
            ))}
          </ul>
          <div id="grupo-empleadores" class="grupo-extra">
            <ul class="grid grid-cols-1 sm:grid-cols-2 gap-3 mt-3">
              {omarEmpleadores.slice(6).map((item) => (
                <li class="bg-white border border-outline-variant rounded-sm p-4 border-l-2 border-l-primary text-[14px] leading-6 text-on-surface">{item}</li>
              ))}
            </ul>
          </div>
          <button data-grupo-toggle aria-expanded="false" aria-controls="grupo-empleadores" class="mt-3 text-sm font-semibold text-primary hover:text-primary-container transition-[opacity,transform] duration-200">Leer más</button>
        </div>

        <div data-grupo="pensionados" class="mb-2">
          <h4 class="text-[18px] font-semibold text-on-surface mb-3">Para pensionados</h4>
          <ul class="grid grid-cols-1 sm:grid-cols-2 gap-3">
            {omarPensionados.slice(0, 6).map((item) => (
              <li class="bg-white border border-outline-variant rounded-sm p-4 border-l-2 border-l-primary text-[14px] leading-6 text-on-surface">{item}</li>
            ))}
          </ul>
          <div id="grupo-pensionados" class="grupo-extra">
            <ul class="grid grid-cols-1 sm:grid-cols-2 gap-3 mt-3">
              {omarPensionados.slice(6).map((item) => (
                <li class="bg-white border border-outline-variant rounded-sm p-4 border-l-2 border-l-primary text-[14px] leading-6 text-on-surface">{item}</li>
              ))}
            </ul>
          </div>
          <button data-grupo-toggle aria-expanded="false" aria-controls="grupo-pensionados" class="mt-3 text-sm font-semibold text-primary hover:text-primary-container transition-[opacity,transform] duration-200">Leer más</button>
        </div>
      </div>

      <!-- Franco Miller Ipial Ipial — Penal §4 -->
      <div data-abogado="franco">
        <h3 class="text-[22px] font-semibold text-on-surface mb-1">Franco Miller Ipial Ipial</h3>
        <p class="text-xs font-semibold text-primary tracking-widest uppercase mb-4">Especialista en Derecho Penal y Procesal Penal</p>
        <div data-grupo="penal">
          <h4 class="text-[18px] font-semibold text-on-surface mb-3">Servicios de Derecho Penal</h4>
          <ul class="grid grid-cols-1 sm:grid-cols-2 gap-3">
            {francoPenal.map((item) => (
              <li class="bg-white border border-outline-variant rounded-sm p-4 border-l-2 border-l-primary text-[14px] leading-6 text-on-surface">{item}</li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  (() => {
    const toggles = document.querySelectorAll('[data-grupo-toggle]');
    toggles.forEach((btn) => {
      btn.addEventListener('click', () => {
        const id = btn.getAttribute('aria-controls');
        const panel = id ? document.getElementById(id) : null;
        const expanded = btn.getAttribute('aria-expanded') === 'true';
        btn.setAttribute('aria-expanded', String(!expanded));
        if (panel) panel.classList.toggle('is-expanded', !expanded);
        btn.textContent = expanded ? 'Leer más' : 'Leer menos';
      });
    });
  })();
</script>